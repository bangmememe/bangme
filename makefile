TARGET = infinit limit celsius2fahr fahr2celsius charNumber triagngle

all : ${TARGET}

infinit : $@
limit : $@
celsius2fair : $@
fahr2celsius : $@
charNumber : $@
triangle : $@

clean :
	rm ${TARGET}
